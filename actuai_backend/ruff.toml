# Linting config — keep it pragmatic, not pedantic.
line-length = 100
target-version = "py313"
[lint]
select = ["E", "F", "I", "B"]
ignore = ["E501", "B008"]  # B008: FastAPI Depends() in defaults is intentional
