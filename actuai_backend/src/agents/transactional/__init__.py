"""Package for the 'transactional' agent.

Implementation lives in the dedicated module `transactional.py` (this `__init__.py` is
intentionally empty). Import from `agents.transactional.transactional`.
"""
